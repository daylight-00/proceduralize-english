---
description: Fast retrieval drill — prompt, 20s response, recast, re-elicit. 10 reps default.
argument-hint: [target or topic]
---

Run `/speaking-drill` per CLAUDE.md §13. Default 10 reps, 20s per response, target chunks in `chunks.md` first, then Active Targets. If $ARGUMENTS is given, scope the drill to that focus. Apply the §5 correction loop on each rep.
